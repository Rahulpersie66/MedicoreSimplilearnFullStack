import { MedicoreUserRegistered } from './medicore-user-registered';

describe('MedicoreUserRegistered', () => {
  it('should create an instance', () => {
    expect(new MedicoreUserRegistered()).toBeTruthy();
  });
});
