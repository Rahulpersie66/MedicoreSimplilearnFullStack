import { MedicoreOrderBooked } from './medicore-order-booked';

describe('MedicoreOrderBooked', () => {
  it('should create an instance', () => {
    expect(new MedicoreOrderBooked()).toBeTruthy();
  });
});
